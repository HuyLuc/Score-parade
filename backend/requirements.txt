# FastAPI và dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-dotenv>=1.0.0

# AI/ML (từ requirements.txt cũ)
opencv-python>=4.8.0
numpy<2.0
scipy>=1.10.0
matplotlib>=3.7.0
ultralytics>=8.0.0  # Cho YOLOv8 (legacy, có thể bỏ nếu không dùng)
filterpy>=1.4.5
scikit-learn>=1.3.0
dtw-python>=1.3.0
fastdtw>=0.3.4
ffmpeg-python>=0.2.0
tqdm>=4.65.0
pillow>=10.0.0
pyyaml>=6.0
torch>=2.0.0
torchvision>=0.15.0

# MMPose - Khuyến nghị sử dụng cho pose estimation
# Cài đặt: pip install openmim && mim install mmengine mmcv mmpose
# Hoặc: pip install mmengine mmcv mmpose (nếu không dùng mim)
# Lưu ý: mmcv cần version phù hợp với torch version
# mmpose>=1.0.0  # Uncomment nếu cài trực tiếp (không dùng mim)

# AlphaPose - Tùy chọn (phức tạp hơn để setup)
# AlphaPose cần clone từ GitHub và setup riêng
# git clone https://github.com/MVIG-SJTU/AlphaPose.git
# pip install -r AlphaPose/requirements.txt

# Utilities
pydantic-settings>=2.0.0
pandas>=2.0.0
openpyxl>=3.1.0  # For Excel .xlsx reading
xlrd<2.0  # For Excel .xls reading (Excel 97-2003 format)

# Audio processing for beat detection
librosa>=0.10.0
soundfile>=0.12.1  # Required by librosa

# Object storage client (MinIO)
minio>=7.2.0

