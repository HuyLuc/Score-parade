# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0,<2.0  # Giới hạn <2.0 để tương thích với torch và các package khác
scipy>=1.10.0
matplotlib>=3.7.0

# Pose estimation
# Option 1: RTMPose (phức tạp hơn, cần mmcv-full) - Đã comment vì có vấn đề với chumpy
# mmpose>=1.0.0
# mmcv-full>=2.0.0  # Sử dụng mmcv-full thay vì mmcv
# mmengine>=0.8.0

# Option 2: YOLOv8-Pose (đơn giản hơn, khuyến nghị) - Đang sử dụng
ultralytics>=8.0.0  # For YOLOv8-Pose

# Multi-person tracking
filterpy>=1.4.5  # For Kalman filter in tracking
scikit-learn>=1.3.0  # For some utilities

# Temporal alignment
dtw-python>=1.3.0

# Video processing
ffmpeg-python>=0.2.0

# Utilities
tqdm>=4.65.0
pillow>=10.0.0
pyyaml>=6.0

# Optional: for better performance (nếu chưa có)
# torch>=2.0.0
# torchvision>=0.15.0

